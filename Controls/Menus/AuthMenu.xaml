<UserControl x:Class="MVIOperationsSystem.Controls.Menus.AuthMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVIOperationsSystem.Controls.Menus"
             xmlns:sf="http://schemas.syncfusion.com/wpf"  
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="800"
             DataContext="{Binding AuthMenuViewModel, Source={StaticResource Locator}}">
    

    <Grid Background="Black" >
        <!--<Grid.RowDefinitions >
            <RowDefinition Height="25" ></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <sf:MenuAdv Grid.Row="0" Grid.Column="0" Background="Black" Foreground="White">
            <sf:MenuItemAdv Header="Sign In" Command="{Binding LoginCommand, Mode=TwoWay}" Background="Black" Foreground="White"/>
            <sf:MenuItemAdv Header="Sign Out" Background="Black" Foreground="White"/>
        </sf:MenuAdv>-->
    </Grid>
</UserControl>
